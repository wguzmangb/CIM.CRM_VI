@model CIM.CRM.Models.Contacto
@{
    var nombreCompleto = $"{Model.Nombre} {Model.Apellidos}".Trim();

    ViewData["Title"] = nombreCompleto;
    ViewData["Titulo"] = nombreCompleto;
    ViewData["Subtitulo"] = "Ficha del contacto";
}

@section Styles {
    <link rel="stylesheet" href="~/css/contactos.css" asp-append-version="true" />
}

<div class="cim-card">
    <div class="cim-card-encabezado">
        <h2>Datos generales</h2>
        <div class="d-flex gap-2 align-items-center">
            <a class="btn-mini" asp-action="Edit" asp-route-id="@Model.ContactoId">Editar</a>
            <a class="liga" asp-action="Index">Volver a la lista</a>
        </div>
    </div>

    <div class="cim-card-cuerpo">
        <div class="ficha">
            <div class="campo">
                <span class="etiqueta">@Html.DisplayNameFor(m => m.Empresa)</span>
                <span class="valor @(Model.Empresa == null ? "vacio" : "")">
                    @if (Model.Empresa != null)
                    {
                        <a asp-controller="Empresas" asp-action="Details"
                           asp-route-id="@Model.EmpresaId">@Model.Empresa.Nombre</a>
                    }
                    else
                    {
                        <text>—</text>
                    }
                </span>
            </div>

            <div class="campo">
                <span class="etiqueta">@Html.DisplayNameFor(m => m.Puesto)</span>
                <span class="valor @(string.IsNullOrWhiteSpace(Model.Puesto) ? "vacio" : "")">
                    @(string.IsNullOrWhiteSpace(Model.Puesto) ? "—" : Model.Puesto)
                </span>
            </div>

            <div class="campo">
                <span class="etiqueta">@Html.DisplayNameFor(m => m.Telefono)</span>
                <span class="valor @(string.IsNullOrWhiteSpace(Model.Telefono) ? "vacio" : "")">
                    @if (string.IsNullOrWhiteSpace(Model.Telefono))
                    {
                        <text>—</text>
                    }
                    else
                    {
                        <a href="tel:@Model.Telefono">@Model.Telefono</a>
                    }
                </span>
            </div>

            <div class="campo">
                <span class="etiqueta">@Html.DisplayNameFor(m => m.Email)</span>
                <span class="valor @(string.IsNullOrWhiteSpace(Model.Email) ? "vacio" : "")">
                    @if (string.IsNullOrWhiteSpace(Model.Email))
                    {
                        <text>—</text>
                    }
                    else
                    {
                        <a href="mailto:@Model.Email">@Model.Email</a>
                    }
                </span>
            </div>

            <div class="campo">
                <span class="etiqueta">@Html.DisplayNameFor(m => m.FechaRegistro)</span>
                <span class="valor">@Model.FechaRegistro.ToString("dd/MM/yyyy HH:mm")</span>
            </div>
        </div>
    </div>
</div>